let selected = document.querySelector("#selected_D")
let select_btn = document.querySelectorAll("")

select_btn.addEventListener('click', function(){
  // if (selected.className == 'selected_D'){
  //   selected.classList.add('full')
  // }else{
  //   selected.className = 'selected_D'
  // }
  console.log(select_btn.innerHTML)
})
